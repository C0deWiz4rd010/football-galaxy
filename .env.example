# No API key is required for the default TheSportsDB free-tier access.
# Configure a local or deployed proxy before enabling live browser mode.
# Example: http://localhost:8787/api/live
VITE_LIVE_DATA_PROXY_URL=
